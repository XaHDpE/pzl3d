using UnityEngine;

public class Dockable : MonoBehaviour
{
    public void SwipeUp()
    {
        /*
        _topDefaultPos = refs.mainCam.GetComponent<Camera>().ViewportToWorldPoint(
            new Vector3(0.5f, 0.8f, ScreenConstants.DefaultDepth));
        // _journeyUpLength = Vector3.Distance(_carouselObjects[chosenObject].position, _topDefaultPos);
			
        TransformHelper.MoveTo(_carouselObjects[chosenObject], _topDefaultPos, 7);
        TransformHelper.ScaleTo(_carouselObjects[chosenObject], _smallSize, 7);
        */

    }
}
